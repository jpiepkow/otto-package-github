{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schema.default.github.issue_create_input",
  "title": "Create GitHub Issue Input",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "repo",
    "title"
  ],
  "properties": {
    "repo": {
      "$ref": "#/$defs/repository"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "body": {
      "type": "string",
      "maxLength": 65536
    }
  },
  "$defs": {
    "repository": {
      "type": "string",
      "minLength": 3,
      "maxLength": 200,
      "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$"
    }
  }
}
